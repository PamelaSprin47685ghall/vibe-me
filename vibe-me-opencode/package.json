{
  "name": "oc-kunwei",
  "version": "0.1.0",
  "description": "OpenCode plugin: basher, editor, explorer, reverie, nudge-todo, ollama-web, refer-caps, loop",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target node --format esm --external @kreuzberg/tree-sitter-language-pack && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/",
    "lint": "bun x biome lint src/",
    "format": "bun x biome format --write src/",
    "check": "bun x biome check --write src/",
    "check:ci": "bun x biome check src/",
    "prepublishOnly": "bun run typecheck && bun run test && bun run check:ci && bun run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@kreuzberg/tree-sitter-language-pack": "^1.8.1",
    "@opencode-ai/plugin": "^1.15.12",
    "engine": "file:../vibe-me-engine"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^25.9.1",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}