{
  "name": "oc-kunwei",
  "version": "0.1.0",
  "description": "OpenCode plugin: basher, editor, explorer, reverie, nudge-todo, ollama-web, refer-caps, loop",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:@opencode-ai/plugin && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src/ --testTimeout 30000",
    "lint": "pnpm exec biome lint src/",
    "format": "pnpm exec biome format --write src/",
    "check": "pnpm exec biome check --write src/",
    "check:ci": "pnpm exec biome check src/",
    "prepublishOnly": "pnpm run typecheck && pnpm run test && pnpm run check:ci && pnpm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@kreuzberg/tree-sitter-language-pack": "^1.8.1",
    "@opencode-ai/plugin": "^1.15.12",
    "engine": "file:../vibe-me-engine"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^25.9.1",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3",
    "vitest": "^3.0.0"
  }
}