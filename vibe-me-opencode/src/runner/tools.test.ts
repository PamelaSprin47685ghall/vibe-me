// Tests refactored into:
// - execute.test.ts        — execute
// - pipe-strip.test.ts     — head/tail pipe stripping
// - wait.test.ts           — wait
// - abort.test.ts          — abort + cleanupJob
// - javascript-prelude.test.ts — createJavascriptPrelude + resolveJavascriptSpecifier + rewriteJavascriptModuleSpecifiers
